# راهنمای پروفایل‌ها

این راهنما سه پروفایل بهینه‌سازی موجود در Backhaul Bulk Generator را توضیح می‌دهد.

---

## 🎯 سه پروفایل

Generator از سه پروفایل بهینه‌سازی پشتیبانی می‌کند:

1. **Speed** - سرعت حداکثر
2. **Stable** - پایداری حداکثر
3. **Balanced** - تعادل بین سرعت و پایداری

---

## 🚀 Speed Profile

### مشخصات:
```toml
channel_size = 4096
heartbeat = 20
mux_con = 128
connection_pool = 16
aggressive_pool = true  # فقط tcp/tcpmux
mtu = 1400
```

### مزایا:
✅ حداکثر پهنای باند  
✅ سریع‌ترین سرعت دانلود/آپلود  
✅ Buffer بزرگ‌تر برای throughput بالا  
✅ Connection pool بیشتر  

### معایب:
⚠️ مصرف منابع بیشتر  
⚠️ ممکن است در شبکه‌های ناپایدار مشکل داشته باشد  

### مناسب برای:
- دانلود فایل‌های بزرگ
- استریم ویدیو با کیفیت بالا
- Bandwidth-intensive applications
- سرورهای قدرتمند
- شبکه‌های پایدار

### نامناسب برای:
- شبکه‌های ناپایدار
- سرورهای ضعیف
- تعداد کاربر خیلی بالا

### نمونه استفاده:
```json
{
  "settings": {
    "profiles": ["speed"]
  }
}
```

---

## 🛡️ Stable Profile

### مشخصات:
```toml
channel_size = 2048
heartbeat = 40
mux_con = 64
connection_pool = 8
aggressive_pool = false
mtu = 1400
```

### مزایا:
✅ پایداری بالا در شبکه‌های ناپایدار  
✅ Heartbeat طولانی‌تر  
✅ مصرف منابع کمتر  
✅ مناسب برای اتصالات بلندمدت  

### معایب:
⚠️ سرعت کمتر از Speed  
⚠️ Throughput محدودتر  

### مناسب برای:
- شبکه‌های ناپایدار (نت‌ملی، اختلالات)
- تانل‌های مهم و حیاتی
- اتصالات بلندمدت
- سرورهای ضعیف‌تر
- محیط‌های production

### نامناسب برای:
- نیاز به سرعت خیلی بالا
- محیط‌های با شبکه پایدار

### نمونه استفاده:
```json
{
  "settings": {
    "profiles": ["stable"]
  }
}
```

---

## ⚖️ Balanced Profile

### مشخصات:
```toml
channel_size = 2048
heartbeat = 20
mux_con = 64
connection_pool = 8
aggressive_pool = false
mtu = 1400
```

### مزایا:
✅ تعادل عالی بین سرعت و پایداری  
✅ مصرف منابع معقول  
✅ مناسب برای اکثر use case ها  
✅ پیش‌فرض توصیه شده  

### معایب:
⚠️ نه خیلی سریع، نه خیلی stable  
⚠️ ممکن است برای موارد خاص بهینه نباشد  

### مناسب برای:
- استفاده روزمره
- اکثر سناریوها
- شروع کار با Backhaul
- محیط‌های مختلط
- پنل‌های VPN عمومی

### نامناسب برای:
- موارد خاص که نیاز به تنظیم دقیق دارند

### نمونه استفاده:
```json
{
  "settings": {
    "profiles": ["balanced"]
  }
}
```

---

## 📊 مقایسه پروفایل‌ها

| ویژگی | Speed | Stable | Balanced |
|-------|-------|--------|----------|
| **Channel Size** | 4096 | 2048 | 2048 |
| **Heartbeat** | 20s | 40s | 20s |
| **Mux Con** | 128 | 64 | 64 |
| **Connection Pool** | 16 | 8 | 8 |
| **Aggressive Pool** | Yes | No | No |
| **MTU** | 1400 | 1400 | 1400 |
| | | | |
| **سرعت** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **پایداری** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **مصرف منابع** | بالا | کم | متوسط |

---

## 🎯 توصیه‌ها

### برای شروع:
```json
"profiles": ["balanced"]
```

شروع با **Balanced** و در صورت نیاز به تنظیمات خاص:
- اگر سرعت مهم است → **Speed**
- اگر پایداری مهم است → **Stable**

### برای تست همه:
```json
"profiles": ["speed", "stable", "balanced"]
```

این کار **سه برابر** تعداد config ها را ایجاد می‌کند!

### توصیه بر اساس شبکه:

#### شبکه پایدار (خارج از ایران):
```
1. Speed (اول امتحان کنید)
2. Balanced (اگر Speed مشکل داشت)
```

#### شبکه ناپایدار (ایران):
```
1. Balanced (شروع)
2. Stable (اگر قطع و وصل دارید)
3. Speed (اگر شبکه خیلی خوب شد)
```

#### شرایط بحرانی (نت‌ملی):
```
1. Stable + xtcpmux/xwsmux
2. Balanced + xtcpmux/xwsmux
```

---

## 🔧 تنظیمات پیشرفته

اگر می‌خواهید پروفایل‌های سفارشی بسازید:

### ویرایش generator.py:

```python
self.profiles = {
    "custom": {
        "channel_size": 3072,
        "heartbeat": 30,
        "mux_con": 96,
        "connection_pool": 12,
        "aggressive_pool": False,
        "mtu": 1400
    }
}
```

### استفاده در config.json:

```json
"profiles": ["custom"]
```

---

## 🧪 تست و بهینه‌سازی

### روش تست:

1. **ساخت همه پروفایل‌ها:**
```json
"profiles": ["speed", "stable", "balanced"]
```

2. **تست سرعت با iperf3:**
```bash
# برای هر config
iperf3 -c 127.0.0.1 -p PORT -t 30
```

3. **مانیتور پایداری:**
```bash
# چک logs
journalctl -u SERVICE_NAME -f
```

4. **انتخاب بهترین:**
بر اساس نتایج، بهترین profile را انتخاب کنید.

### معیارهای ارزیابی:

- **سرعت:** Mbps از iperf3
- **پایداری:** Uptime و تعداد reconnect
- **Latency:** Ping time
- **CPU Usage:** Load average
- **Memory:** RAM usage

---

## 💡 نکات مهم

### ۱. Transport + Profile
Profile با transport ترکیب می‌شود:

```
tcp-speed       → سریع‌ترین
tcpmux-stable   → باثبات‌ترین
utcpmux-balanced → بهترین برای multi-user
```

### ۲. مصرف Disk
هر profile یک config جداگانه:

```
3 profiles × 21 transports = 63 configs per connection!
```

### ۳. مدیریت
همه profile ها service جداگانه دارند:

```bash
systemctl status backhaul-iran100-tcp-speed
systemctl status backhaul-iran101-tcp-stable
systemctl status backhaul-iran102-tcp-balanced
```

---

## ❓ سوالات متداول

### Q: کدوم profile رو باید انتخاب کنم؟

**A:** شروع با `balanced`. اگر مشکل داشتید:
- سرعت پایین → `speed`
- قطع و وصل → `stable`

### Q: میشه همزمان همه رو استفاده کنم?

**A:** بله! ولی فقط یک profile برای هر transport در یک زمان باید active باشد.

### Q: تفاوت واقعی چقدر است؟

**A:** بستگی به شبکه دارد. در برخی موارد:
- Speed: 500 Mbps
- Balanced: 450 Mbps
- Stable: 400 Mbps

### Q: Profile برای TUN transports تأثیر دارد؟

**A:** بله، ولی کمتر از transport های معمولی. فقط MTU یکسان است (1400).

---

**توصیه نهایی:** شروع با `balanced` و بعد تست profile های دیگر در صورت نیاز! 🎯
