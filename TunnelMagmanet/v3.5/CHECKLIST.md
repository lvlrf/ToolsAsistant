# ✅ Checklist: جلوگیری از خطا در پروژه‌های آینده

این چک‌لیست برای اطمینان از کیفیت کد و کاهش خطاها است.

---

## 📋 قبل از شروع کدنویسی

### ۱. درک Requirements
- [ ] تمام requirements را یادداشت کنید
- [ ] نمونه config/input واقعی از کاربر بگیرید
- [ ] edge cases احتمالی را شناسایی کنید
- [ ] با کاربر تایید کنید که درست متوجه شدید

### ۲. طراحی
- [ ] ساختار کلی را طرح‌ریزی کنید
- [ ] ورودی‌ها و خروجی‌ها را مشخص کنید
- [ ] حالت‌های خاص را در نظر بگیرید

---

## 💻 حین کدنویسی

### ۱. Input Validation
- [ ] تمام ورودی‌ها را validate کنید
- [ ] برای ورودی‌های غیرمنتظره error handling داشته باشید
- [ ] پیام‌های خطا واضح و مفید باشند

### ۲. Edge Cases
```python
# مثال: چک کردن حالت‌های مختلف
if transports == "all":
    # حالت string
elif transports == ["all"]:
    # حالت list با یک عضو
elif isinstance(transports, list):
    # حالت list عادی
else:
    # حالت غیرمنتظره
    raise ValueError("Invalid transports format")
```

### ۳. Type Checking
```python
# استفاده از type hints
def process_config(config: dict) -> list:
    transports: Union[str, List[str]] = config.get("transports", [])
```

---

## 🧪 قبل از تحویل

### ۱. تست با Config واقعی
- [ ] با config نمونه ای که کاربر داد تست کنید
- [ ] حالت‌های مختلف ورودی را امتحان کنید:
  - [ ] `"transports": "all"`
  - [ ] `"transports": ["all"]`
  - [ ] `"transports": ["tcp", "ws"]`
  - [ ] لیست خالی
  - [ ] مقادیر null/undefined

### ۲. چک Output
- [ ] خروجی‌ها را manually بررسی کنید
- [ ] فایل‌های تولید شده را باز کنید
- [ ] syntax و format را چک کنید

### ۳. مستندات
- [ ] README کامل باشد
- [ ] مثال‌های عملی داشته باشد
- [ ] Troubleshooting section داشته باشد

### ۴. Clean Code
- [ ] کامنت‌های مفید
- [ ] نام‌گذاری واضح
- [ ] کد تکراری نداشته باشید

---

## 🎯 بعد از تحویل

### ۱. بازخورد کاربر
- [ ] بازخورد اولیه کاربر را بگیرید
- [ ] باگ‌های گزارش شده را فوری fix کنید
- [ ] پیشنهادات را یادداشت کنید

### ۲. آپدیت
- [ ] نسخه‌ها را مستند کنید
- [ ] تغییرات را در CHANGELOG بنویسید

---

## 🔍 نمونه Test Cases

### برای این پروژه (Backhaul Generator):

```python
# Test Case 1: String "all"
{
    "transports": "all"
}
# Expected: همه transport ها

# Test Case 2: List با "all"
{
    "transports": ["all"]
}
# Expected: همه transport ها

# Test Case 3: List خاص
{
    "transports": ["tcp", "ws", "tcpmux"]
}
# Expected: فقط این سه transport

# Test Case 4: List خالی
{
    "transports": []
}
# Expected: هیچ config ای ساخته نشه یا warning

# Test Case 5: transport نامعتبر
{
    "transports": ["invalid_transport"]
}
# Expected: Warning و skip شدن

# Test Case 6: profile نامعتبر
{
    "settings": {
        "profiles": ["invalid_profile"]
    }
}
# Expected: Error با پیام واضح
```

---

## 📝 Template گزارش باگ

وقتی کاربر باگ گزارش می‌کند:

```
### باگ:
[توضیح مشکل]

### Config استفاده شده:
```json
[کپی config.json]
```

### خطای دریافتی:
```
[کپی error message]
```

### محیط:
- OS: [Windows/Linux/Mac]
- Python Version: [3.x]
- Generator Version: [v1.0]

### مراحل بازتولید:
1. [مرحله اول]
2. [مرحله دوم]
3. ...

### رفتار مورد انتظار:
[چه اتفاقی باید می‌افتاد]

### رفتار واقعی:
[چه اتفاقی افتاد]
```

---

## 🚀 بهبودهای پیشنهادی

### برای این پروژه:
1. ✅ Unit Tests اضافه کردن
2. ✅ CI/CD برای تست خودکار
3. ✅ Logging بهتر
4. ✅ Progress bar برای تولید config های زیاد
5. ✅ Dry-run mode برای preview قبل از ساخت

---

## 💡 یادآوری‌های مهم

### برای Developer (من):
> ❗ همیشه قبل از تحویل با config واقعی کاربر تست کن!

> ❗ Edge cases را فراموش نکن!

> ❗ Error messages واضح و مفید باشند!

### برای کاربر (شما):
> ✅ نمونه config واقعی خودتان را بدهید

> ✅ بعد از دریافت، سریع تست کنید

> ✅ باگ‌ها را با جزئیات گزارش دهید

---

**با رعایت این checklist، خطاها به حداقل می‌رسد!** 🎯
