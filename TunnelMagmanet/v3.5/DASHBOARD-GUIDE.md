# راهنمای استفاده از Dashboard

## 🎯 مقدمه

Dashboard یک رابط کاربری تحت وب برای مدیریت راحت‌تر کانفیگ‌های Backhaul است.

---

## 🚀 راه‌اندازی

### روش ۱: Standalone (توصیه می‌شود)

```bash
# بعد از اجرای generator.py
python3 generate-dashboard.py
```

سپس فایل `dashboard-standalone.html` را دابل کلیک کنید یا در مرورگر باز کنید.

### روش ۲: با Web Server

```bash
# در پوشه اصلی
python3 -m http.server 8000

# در مرورگر:
http://localhost:8000/dashboard.html
```

---

## 📊 امکانات Dashboard

### ۱. نمای کلی (Stats)

در بالای صفحه:
- **کل کانفیگ‌ها:** تعداد کل config های ساخته شده
- **تعداد Connection ها:** تعداد ارتباطات بین سرورها
- **نوع Transport:** تعداد انواع transport استفاده شده
- **نوع پروفایل:** تعداد پروفایل‌های استفاده شده

---

### ۲. عملیات کلی

دکمه‌های زیر دستورات مربوطه را کپی می‌کنند:

#### 🔄 ری‌استارت همه سرویس‌ها
```bash
cd /root/backhaul-core && bash restart-services.sh
```

#### ⏸️ توقف همه سرویس‌ها
```bash
cd /root/backhaul-core && bash stop-services.sh
```

#### ▶️ استارت همه سرویس‌ها
```bash
cd /root/backhaul-core && bash install-services.sh
```

#### 🗑️ حذف همه سرویس‌ها
```bash
cd /root/backhaul-core && bash remove-services.sh
```

---

### ۳. فیلترها

#### جستجو
می‌توانید بر اساس موارد زیر جستجو کنید:
- نام سرور (Iran یا Kharej)
- شماره پورت
- نوع Transport
- نوع Profile

#### فیلتر سرور
فقط کانفیگ‌های یک سرور خاص را نمایش دهید.

#### فیلتر Transport
فقط یک نوع transport خاص را نمایش دهید.

#### فیلتر Profile
فقط یک profile خاص (speed, stable, balanced) را نمایش دهید.

---

### ۴. کارت‌های Config

هر کانفیگ یک کارت دارد که شامل:

#### اطلاعات:
- **نام سرویس:** مثل `backhaul-iran100-tcp-speed`
- **Connection:** مثل `Tehran-Main → Germany-Hetzner`
- **Transport:** مثل `TCP-SPEED` یا `TCPMUX-V2-STABLE`
- **پورت تانل:** پورت اصلی تانل
- **پورت وب:** پورت Web Panel
- **پورت iperf:** پورت تست سرعت
- **Subnet:** (فقط برای TUN transports)

#### دکمه‌های عملیاتی:

##### 🌐 وب پنل Iran / Kharej
آدرس Web Panel را کپی می‌کند:
```
http://SERVER_IP:WEB_PORT
```

**نکته:** باید `SERVER_IP` را با آی‌پی واقعی سرور جایگزین کنید.

##### 📊 وضعیت Iran / Kharej
دستور چک وضعیت سرویس:
```bash
systemctl status backhaul-iran100-tcp-speed.service
```

##### 🔄 ری‌استارت Iran / Kharej
دستور ری‌استارت سرویس:
```bash
systemctl restart backhaul-iran100-tcp-speed.service
```

##### 📜 لاگ Iran / Kharej
دستور مشاهده لاگ real-time:
```bash
journalctl -u backhaul-iran100-tcp-speed.service -f
```

---

## 🎯 سناریوهای استفاده

### سناریو ۱: چک کردن همه سرویس‌های یک سرور

1. از فیلتر **سرور** سرور مورد نظر را انتخاب کنید
2. روی دکمه **📊 وضعیت** هر کانفیگ کلیک کنید
3. دستور در clipboard کپی می‌شود
4. در ترمینال SSH paste کنید

### سناریو ۲: ری‌استارت یک transport خاص

1. از فیلتر **Transport** نوع مورد نظر (مثلاً TCP) را انتخاب کنید
2. روی دکمه **🔄 ری‌استارت** هر کانفیگ کلیک کنید
3. دستور را در ترمینال اجرا کنید

### سناریو ۳: مشاهده لاگ‌های یک profile

1. از فیلتر **Profile** یک profile (مثلاً Speed) را انتخاب کنید
2. روی دکمه **📜 لاگ** کلیک کنید
3. لاگ real-time را در ترمینال مشاهده کنید

### سناریو ۴: باز کردن Web Panel ها

1. روی دکمه **🌐 وب پنل Iran** کلیک کنید
2. آدرس کپی می‌شود: `http://SERVER_IP:800`
3. `SERVER_IP` را با آی‌پی سرور جایگزین کنید
4. در مرورگر باز کنید

### سناریو ۵: ری‌استارت کلی سیستم

1. روی دکمه **🔄 ری‌استارت همه سرویس‌ها** کلیک کنید
2. دستور کپی می‌شود
3. در ترمینال سرور (Iran یا Kharej) paste کنید

---

## 💡 نکات مهم

### ۱. فایل Standalone
فایل `dashboard-standalone.html` تمام داده‌ها را داخل خودش دارد و نیاز به web server ندارد.

### ۲. آپدیت Dashboard
هر بار که `generator.py` را اجرا می‌کنید، باید دوباره dashboard را بسازید:
```bash
python3 generate-dashboard.py
```

### ۳. Web Panel Ports
پورت‌های Web Panel به صورت خودکار از `800` شروع می‌شوند:
- Iran 100: port 800
- Iran 101: port 801
- Iran 102: port 802
- ...

### ۴. دستورات فقط کپی می‌شوند
Dashboard فقط دستورات را کپی می‌کند و اجرا نمی‌کند. شما باید:
1. دستور را کپی کنید
2. در ترمینال SSH سرور paste کنید
3. Enter بزنید

### ۵. IP سرورها
در dashboard باید IP سرورها را خودتان در دستورات جایگزین کنید.

---

## 🎨 طراحی Responsive

Dashboard روی تمام دستگاه‌ها کار می‌کند:
- 💻 Desktop
- 📱 Tablet
- 📱 Mobile

---

## 🔧 عیب‌یابی

### Dashboard خالی است
✅ **راه‌حل:** ابتدا `generator.py` را اجرا کنید تا `state.json` ساخته شود.

### فیلترها کار نمی‌کنند
✅ **راه‌حل:** از مرورگر مدرن (Chrome, Firefox, Edge) استفاده کنید.

### دکمه کپی کار نمی‌کند
✅ **راه‌حل:** به مرورگر اجازه دسترسی به clipboard بدهید.

---

## 🚀 ویژگی‌های آینده

- [ ] اجرای مستقیم دستورات از dashboard
- [ ] نمایش وضعیت real-time سرویس‌ها
- [ ] نمایش نمودار ترافیک
- [ ] Export به Excel/CSV
- [ ] مدیریت چند سرور همزمان

---

**از Dashboard لذت ببرید!** 🎉
